import type { Meeting, SummaryTemplate } from "@/lib/data";
const labels: SummaryTemplate[] = ["general", "sales", "interview", "standup"];
export function SummaryPanel({ meeting, template = "general" }: { meeting: Meeting; template?: SummaryTemplate }) { const summary = meeting.summaries.find((s) => s.template === template) ?? meeting.summaries[0]; return <section className="card"><div className="tabs">{labels.map((label) => <a className={`tab ${label === summary.template ? "active" : ""}`} href={`?summary=${label}`} key={label}>{label}</a>)}</div><h2>{summary.content.headline}</h2><h3>Key Points</h3><ul>{summary.content.bullets.map((b) => <li key={b}>{b}</li>)}</ul><h3>Decisions</h3><ul>{summary.content.decisions.map((d) => <li key={d}>{d}</li>)}</ul><h3>Key Quotes</h3><ul>{summary.content.keyQuotes.map((q) => <li key={q}>"{q}"</li>)}</ul></section>; }
