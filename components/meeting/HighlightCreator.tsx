import type { Meeting } from "@/lib/data";
export function HighlightCreator({ meeting }: { meeting: Meeting }) { return <section className="card"><h2>Highlights</h2><div className="grid">{meeting.highlights.map((h) => <a className="segment" href={`/share/${h.shareToken}`} key={h.id}><strong>{Math.floor(h.startMs / 60000)}:{String(Math.floor(h.startMs / 1000) % 60).padStart(2, "0")}</strong><span>{h.title}<br/><span className="muted">Share token ready</span></span></a>)}</div><button className="button primary" style={{marginTop:14}}>Create highlight from selected range</button></section>; }
